# Simple Telegram Bot with grammY

## Setup (TypeScript)

1. Install dependencies:
   ```sh
   npm install
   ```
2. Export your Telegram bot token (replace with real token):
   ```sh
   export BOT_TOKEN=123456:ABCDEF
   ```
3. Start the bot (tsx runs the TypeScript entry):
   ```sh
   npm start
   ```
